<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
       <%= link_to "vehicle tracker", {:controller => "trackers", :action => "view", :user_index => current_user.id}, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Track", {:controller => "trackers", :action => "view", :user_index => current_user.id} %></li>
          <li><%= link_to "How To", help_path %></li>
          <% if signed_in? %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Edit Bounds", {:controller => "trackers", :action => "edit", :user_index => current_user.id} %></li>
                <li><%= link_to "Options", '' %></li>
                <li class="divider"></li>
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>